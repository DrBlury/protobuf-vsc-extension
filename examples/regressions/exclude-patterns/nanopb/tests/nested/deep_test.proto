// This file is in nanopb/tests/nested/ and should be EXCLUDED from "Compile All Protos"

syntax = "proto3";

package nanopb.tests.nested;

// Nested test message - this should NOT be compiled when using exclude patterns
message DeepNestedTest
{
  string deep_field = 1;
}
